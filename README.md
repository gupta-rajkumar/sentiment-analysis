# sentiment-analysis
A basic sentiment analyzer which takes tweets from the twitterAPI analyzes it and and shows how many tweets were in favour of it and how many were not in favour of that particular keyword.

1. Copy your bearer token.
2. Open up the terminal and type the following command.
3. export BEARER_TOKEN=<YOUR BEARER TOKEN>
